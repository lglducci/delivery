CREATE OR REPLACE FUNCTION contab.ff_diario_reabrir_dia(
    p_empresa_id bigint,
    p_data date
)
RETURNS void
LANGUAGE plpgsql
AS $$
BEGIN
    DELETE FROM contab.diario
     WHERE empresa_id = p_empresa_id
       AND data_mov   = p_data
       AND status     = 'rascunho';
END;
$$;
